# Python
venv/
__pycache__/
*.pyc
.env
f1_cache/
backend/f1_cache/
backend/app/f1_cache/

# Track-geometry build cache (GeoJSON + DEM responses + intermediate stages).
# Deleting this is safe but costs OpenTopoData quota to rebuild — see scripts/README.md.
.cache/

# Node
node_modules/
.next/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local working notes, not for the repo
specs.md

# Large asset source folders (these go to GCS, not Git)
f1_driver_images/
f1_2026_flags/
stitch/
stitch.zip
/circuits/
/teams/

# Superpowers session working files (brainstorm mockups, subagent reports)
.superpowers/

# Local API overrides used when pointing a dev server at deployed services
.env.local
